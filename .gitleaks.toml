title = "SevenMirror Server secret scanning"

[extend]
useDefault = true

[[allowlists]]
description = "Published deterministic protocol vectors contain test-only keys and credentials."
paths = ['''^protocol/test-vectors/''']
